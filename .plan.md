# Pi-Chi Dashboard Upgrade Plan

## Goal
Full Pi visibility from the web UI + TV remote navigation on HDMI kiosk.

## Current State
- Dashboard: Next.js 15 on port 3333, Cage+Chromium kiosk on HDMI
- Webcam: Logitech C922 at /dev/video0, `fswebcam` installed, 16+ captures in /tmp/
- Radio: MPD + Icecast2 already installed and active, music dir at /var/lib/mpd/music
- CEC: /dev/cec0 exists, but `cec-utils` not installed
- No ffmpeg installed

---

## Feature 1: Camera Tab (Live Feed + Gallery)

### New Files
1. **`app/api/webcam/snapshot/route.ts`** — Captures a frame from /dev/video0 via `fswebcam` shell command, returns JPEG binary
2. **`app/api/webcam/gallery/route.ts`** — Lists all `/tmp/webcam_*.jpg` files with timestamps and sizes
3. **`app/api/webcam/image/route.ts`** — Serves a specific image file by path (GET with `?path=/tmp/webcam_xxx.jpg`)
4. **`components/agent/camera-panel.tsx`** — Two sub-views:
   - **Live View**: Polls `/api/webcam/snapshot` every 2 seconds, displays as `<img>` with blob URL. Start/stop toggle, capture button (saves to /tmp/), FPS indicator.
   - **Gallery**: Grid of thumbnails from `/api/webcam/gallery`. Click to enlarge (lightbox). Delete button per image. Sort by newest first.

### Modified Files
5. **`components/agent-dashboard.tsx`** — Add `'camera'` to `CenterTab` type, add tab with Camera icon, render `CameraPanel` in tab content area. Lazy-loaded.

---

## Feature 2: Radio Station Tab

### New Files
6. **`app/api/radio/route.ts`** — Unified radio API:
   - `GET` → MPD status (current song, state, volume, playlist)
   - `POST` → Control actions: play, pause, stop, next, prev, volume, add-stream, clear, load-playlist
   - Uses `mpc` CLI commands via shell execution
7. **`components/agent/radio-panel.tsx`** — Radio station UI:
   - **Now Playing**: Song title, artist, album art placeholder, progress bar
   - **Transport Controls**: Play/Pause, Stop, Previous, Next, Volume slider
   - **Playlist**: Current queue with drag-to-reorder (or just list)
   - **Add Stream**: Input field to add internet radio stream URLs
   - **Presets**: Save favorite stations (stored in brain-state or a JSON file)
   - **Icecast Status**: Link to Icecast admin, listener count

### Pi Setup
8. **Configure MPD audio output** — Ensure MPD outputs to HDMI audio (or headphone jack). Configure Icecast output plugin for streaming.
9. **Add some default internet radio streams** to MPD playlist (lofi, jazz, ambient, news).

### Modified Files
10. **`components/agent-dashboard.tsx`** — Add `'radio'` to `CenterTab` type, add tab with Radio icon, render `RadioPanel`. Lazy-loaded.

---

## Feature 3: TV Remote Navigation (Kiosk)

### Pi Setup
11. **Install `cec-utils`**: `sudo apt install -y cec-utils`
12. **Create CEC-to-keyboard bridge script** (`scripts/cec-remote.sh`):
    - Runs `cec-client -d 8` and translates button presses to X keyboard events via `xdotool` or `wtype` (Wayland)
    - Maps: Up/Down/Left/Right arrows, Select→Enter, Back→Escape, Play/Pause/Stop for radio
    - Actually since we're on Cage (Wayland), use `wtype` or `ydotool` for injecting key events
13. **Create systemd service** for CEC bridge (`pi-chi-cec.service`)

### Dashboard UI Changes
14. **`components/agent-dashboard.tsx`** — Add keyboard navigation:
    - Arrow Left/Right to switch center tabs
    - Arrow Up/Down to scroll within active panel
    - Enter to interact with focused elements
    - Escape to go back/close modals
    - Visible focus rings on all interactive elements (thick cyan border)
15. **`app/globals.css`** — Add `.kiosk-focus` styles with large, visible focus indicators suitable for TV viewing (thicker borders, larger text)
16. **Detect kiosk mode** — Check if running on Pi (user-agent or `window.innerWidth === 1920`) and auto-enable TV-optimized navigation

---

## Implementation Order

1. Camera Tab (most requested, straightforward)
2. Radio Tab (MPD/Icecast already installed)
3. TV Remote Navigation (most complex, needs Pi-side setup)

## Build Strategy
- Develop on Windows, build locally with `npm run build`
- SCP the `.next/` folder to Pi (Pi can't compile Next.js — ARM CPU too slow)
- Restart `pi-chi-dashboard` service

## Files Changed Summary
- **New**: 5 API routes, 2 components, 1 CEC bridge script, 1 systemd service
- **Modified**: `agent-dashboard.tsx` (tab additions + keyboard nav), `globals.css` (focus styles)
- **Pi setup**: Install `cec-utils`, configure MPD, add radio streams
