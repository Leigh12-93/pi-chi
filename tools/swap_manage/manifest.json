{
  "name": "swap_manage",
  "description": "Check swap usage, show swap details, and optionally clear swap cache",
  "command": "bash /home/pi/.pi-chi/tools/swap_manage/swap_manage.sh {{action}}",
  "parameters": {
    "action": {
      "type": "string",
      "description": "Action: 'status' to check, 'top' to show top swap consumers, 'clear' to flush swap"
    }
  }
}
