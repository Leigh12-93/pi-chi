{
  "name": "docker_ps",
  "description": "List Docker containers with status, ports, and resource usage",
  "command": "bash /home/pi/.pi-chi/tools/docker_ps/docker_ps.sh {{filter}}",
  "parameters": {
    "filter": {
      "type": "string",
      "description": "Filter: 'all' for all containers, 'running' for running only, or container name to inspect"
    }
  }
}
