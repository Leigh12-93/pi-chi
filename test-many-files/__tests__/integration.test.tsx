import { describe, it, expect } from 'vitest'

describe('Integration Tests', () => {
  it('should load the app', () => {
    expect(true).toBe(true)
  })
})

// This entire __tests__ directory should be filtered out