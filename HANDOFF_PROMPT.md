# 🚀 FORGE HANDOFF PROMPT - Complete Fix List\n\nYou are inheriting a production Next.js 15 + Supabase + Vercel AI website builder called **Forge**. Below are ALL critical issues requiring immediate fixes, with exact file locations and implementation details.\n\n## 🎯 **CRITICAL ISSUES TO FIX**\n\n### 1. 🔴 **PREVIEW SYSTEM BROKEN** (HIGHEST PRIORITY)\n\n**Problem**: Preview panel shows gray screen with empty URL bar. The v0-sandbox system was working but now fails to initialize.\n\n**Root Cause**: The file count check was moved incorrectly during the \"too many files\" fix, breaking the preview initialization flow.\n\n**Files to Fix**:\n- `components/preview-panel.tsx` (line 100-120) - Preview initialization logic\n- `lib/v0-sandbox.ts` (line 450-470) - File filtering logic\n- `app/api/sandbox/route.ts` (line 75-85) - File count validation\n\n**Current Broken Code** (`app/api/sandbox/route.ts`):\n```typescript\nif (fileCount > MAX_FILES) {\n  return timedJson(\n    { error: `Too many files (${fileCount}). Max ${MAX_FILES}.` },\n    { status: 400 },\n    start,\n  )\n}\n```\n\n**Required Fix**:\n1. **Remove** the raw file count check from `/api/sandbox` route\n2. **Move** the limit enforcement to `lib/v0-sandbox.ts` **after** filtering\n3. **Update** `prepareFiles()` to return error if filtered files > 200\n4. **Fix** preview panel auto-start logic to handle new error format\n\n**Implementation Pattern**:\n```typescript\n// In lib/v0-sandbox.ts - prepareFiles()\nif (prepared.length > 200) {\n  return {\n    prepared: [],\n    skipped: files.length,\n    totalSize: 0,\n    error: `Too many useful files (${prepared.length}). Max 200 after filtering. Consider removing test files or documentation.`\n  }\n}\n```\n\n### 2. 🎯 **COMPLETION TOKEN/COST DISPLAY** (COMPLETED)\n\n**Status**: ✅ FIXED - Token count and cost information successfully added to completion display.\n\n**Implementation Completed**:\n- Enhanced `ActivityBlock` interface with `currentResponseCost` prop\n- Updated completion display to show: input tokens, output tokens, estimated cost\n- Made completion messages persistent in chat stream (removed auto-hide)\n- Added animated cost chip styling for consistency\n\n**Result**: Completion lines now show \"✅ Worked for 2.3s · 5 actions 💰 1.2k in · 845 out · $0.023\"\n\n### 3. 📱 **MOBILE CHAT INPUT ISSUES**\n\n**Problem**: Poor mobile experience - keyboard handling, touch targets, smooth transitions.\n\n**Files to Fix**:\n- `components/chat-panel.tsx` (lines 50-80) - Mobile keyboard handling\n- `components/chat/chat-input.tsx` (lines 100-150) - Touch targets\n- `app/globals.css` (lines 800-850) - Mobile responsive styles\n\n**Current Issues**:\n1. iOS keyboard pushes chat input off-screen\n2. Touch targets too small (<44px)\n3. No smooth transitions between states\n4. Font size <16px triggers zoom on iOS\n\n**Required Fixes**:\n```typescript\n// Mobile keyboard handling\nuseEffect(() => {\n  if (typeof window !== 'undefined' && /iPhone|iPad/.test(navigator.userAgent)) {\n    const handleResize = () => {\n      const vh = window.innerHeight * 0.01\n      document.documentElement.style.setProperty('--vh', `${vh}px`)\n    }\n    window.addEventListener('resize', handleResize)\n    handleResize()\n    return () => window.removeEventListener('resize', handleResize)\n  }\n}, [])\n\n// Touch targets\nconst buttonClasses = \"min-h-[44px] min-w-[44px] p-3 sm:p-2 text-base sm:text-sm\"\n\n// Prevent iOS zoom\ninput: {\n  fontSize: '16px', // Prevents iOS zoom\n  touchAction: 'manipulation'\n}\n```\n\n### 4. 🔀 **GIT BRANCH AUTO-DETECTION**\n\n**Problem**: Branch selector hardcoded to \"main\" but project uses \"master\".\n\n**Files to Fix**:\n- `hooks/use-github.ts` (line 45) - Default branch fallback\n- `lib/tools/github-tools.ts` (line 120) - Branch parameter defaults\n- `components/sidebar/github-panel.tsx` (lines 80-90) - Branch display logic\n\n**Current Broken Code** (`lib/tools/github-tools.ts`):\n```typescript\nbranch: branch || 'main'\n```\n\n**Required Fix**:\n1. **Detect default branch** via GitHub API on repository connection\n2. **Cache result** in project settings or local storage\n3. **Update all branch operations** to use detected default\n\n**Implementation Pattern**:\n```typescript\n// Auto-detect default branch\nconst detectDefaultBranch = async (owner: string, repo: string) => {\n  const response = await fetch(`/api/github/repos?owner=${owner}&repo=${repo}`)\n  const data = await response.json()\n  return data.default_branch || 'main'\n}\n\n// Use in branch operations\nconst defaultBranch = await detectDefaultBranch(owner, repo)\nbranch: branch || defaultBranch\n```\n\n### 5. 📲 **DEPLOYMENT MODAL FOR MOBILE**\n\n**Problem**: Deployment happens via sidebar panel, poor mobile UX.\n\n**Files to Fix**:\n- `components/sidebar/vercel-panel.tsx` (lines 200-250) - Deploy interface\n- `components/deploy-modal.tsx` (NEW FILE) - Dedicated mobile modal\n- `components/chat/task-list-panel.tsx` (lines 50-80) - Progress display\n\n**Required Implementation**:\n1. **Create dedicated deployment modal** triggered by deploy action\n2. **Full-screen on mobile** with proper progress indicators\n3. **Build logs display** with scroll and error highlighting\n4. **Cancel option** and retry functionality\n\n**Mobile Modal Pattern**:\n```typescript\nconst DeployModal = ({ isOpen, onClose, projectId }) => {\n  return (\n    <Dialog open={isOpen} onClose={onClose}>\n      <div className=\"fixed inset-0 z-50 sm:p-4\">\n        <div className=\"h-full bg-white sm:h-auto sm:max-h-[600px] sm:rounded-lg\">\n          {/* Full screen on mobile, modal on desktop */}\n        </div>\n      </div>\n    </Dialog>\n  )\n}\n```\n\n### 6. ⚡ **GIT OPERATION LIVE FEEDBACK**\n\n**Problem**: Git operations (push, create repo) happen silently with no progress indication.\n\n**Files to Fix**:\n- `components/sidebar/github-panel.tsx` (lines 150-200) - Operation buttons\n- `hooks/use-github.ts` (lines 80-120) - Operation handlers\n- `lib/tools/github-tools.ts` (lines 200-300) - API calls\n\n**Current Silent Operations**:\n```typescript\n// No progress indication\nconst handlePush = async () => {\n  await pushToGithub(files) // Silent\n}\n```\n\n**Required Live Feedback**:\n```typescript\nconst handlePush = async () => {\n  setGitStatus('pushing')\n  setGitMessage('Preparing files...')\n  \n  try {\n    await pushToGithub(files, {\n      onProgress: (message) => setGitMessage(message)\n    })\n    setGitStatus('success')\n    setGitMessage('Push completed!')\n  } catch (error) {\n    setGitStatus('error')\n    setGitMessage(error.message)\n  }\n}\n```\n\n### 7. ⏱️ **THINKING STATUS TIMING**\n\n**Problem**: \"Thinking...\" status shows too early, hides too late, causes flicker.\n\n**Files to Fix**:\n- `components/chat/chat-message.tsx` (line 65-85) - Thinking indicator\n- `hooks/use-chat.ts` (lines 200-250) - Status management\n- `components/chat/thinking-indicator.tsx` - Animation timing\n\n**Current Flicker Issues**:\n```typescript\n// Shows immediately (jarring)\nsetThinking(true)\n\n// Doesn't hide on first token\nif (response.ok) {\n  setThinking(false) // Too late\n}\n```\n\n**Required Smooth Timing**:\n```typescript\n// Debounced show (300ms delay)\nconst showThinking = useDeferredValue(isProcessing, 300)\n\n// Hide immediately on first response token\nconst reader = response.body?.getReader()\nconst firstChunk = await reader?.read()\nif (firstChunk?.value) {\n  setThinking(false) // Immediate hide\n}\n```\n\n### 8. 📋 **TASK PANEL MOBILE OPTIMIZATION**\n\n**Problem**: Task list panel not optimized for mobile interaction.\n\n**Files to Fix**:\n- `components/chat/task-list-panel.tsx` (lines 100-150) - Mobile layout\n- `app/globals.css` (lines 900-950) - Task panel responsive styles\n\n**Mobile Issues**:\n1. Panel too narrow on mobile\n2. Task items hard to tap\n3. No swipe gestures for dismissal\n4. Progress indicators too small\n\n**Required Mobile Patterns**:\n```css\n@media (max-width: 640px) {\n  .task-panel {\n    bottom: 80px; /* Above mobile keyboard */\n    left: 1rem;\n    right: 1rem;\n    width: auto;\n  }\n  \n  .task-item {\n    min-height: 44px;\n    padding: 12px;\n    font-size: 16px;\n  }\n}\n```\n\n---\n\n## 🏗️ **IMPLEMENTATION PRIORITY**\n\n### **Phase 1: Critical Fixes** (Immediate)\n1. ✅ **Preview System** - Get sandboxes working again\n2. **Git Branch Detection** - Fix hardcoded \"main\" branch\n3. **Mobile Chat Input** - Basic keyboard handling\n\n### **Phase 2: UX Polish** (Next)\n4. **Thinking Status Timing** - Smooth transitions\n5. **Git Live Feedback** - Progress indicators\n6. **Task Panel Mobile** - Responsive optimization\n\n### **Phase 3: Advanced** (Later)\n7. **Deployment Modal** - Dedicated mobile experience\n8. **Additional mobile optimizations**\n\n---\n\n## 🔧 **VERIFICATION STEPS**\n\n### **Preview System Test**:\n1. Create project with 5+ files\n2. Verify preview auto-starts within 10 seconds\n3. Check URL bar shows live path\n4. Confirm iframe loads correctly\n\n### **Git Branch Test**:\n1. Connect to repository using \"master\" branch\n2. Verify branch selector shows \"master\" not \"main\"\n3. Test push operations use correct branch\n\n### **Mobile Chat Test**:\n1. Test on iOS Safari and Chrome\n2. Verify keyboard doesn't push input off-screen\n3. Check all buttons are tappable (44px minimum)\n4. Confirm no zoom on input focus\n\n### **Status Timing Test**:\n1. Send message and observe thinking indicator\n2. Should show after 300ms delay\n3. Should hide immediately when response starts\n4. No flicker between states\n\n---\n\n## 📁 **PROJECT CONTEXT**\n\n- **Framework**: Next.js 15 + TypeScript\n- **Database**: Supabase PostgreSQL\n- **Styling**: Tailwind CSS + Custom CSS\n- **State**: React hooks + Context\n- **Preview**: v0-sandbox + static HTML fallback\n- **Git**: GitHub API integration\n- **Deploy**: Vercel API\n- **Auth**: GitHub OAuth via Supabase\n\n**Key Patterns**:\n- Use `cn()` utility for conditional classes\n- Prefer hooks over Redux/Zustand\n- Mobile-first responsive design\n- Error boundaries for graceful failures\n- TypeScript strict mode enabled\n\n**Success Criteria**:\n- All preview sandboxes auto-start within 10 seconds\n- Git operations show live progress feedback\n- Mobile experience feels native (no awkward interactions)\n- Status indicators smooth and predictable\n- Zero \"main\" vs \"master\" branch conflicts\n\nImplement these fixes in the exact order listed, testing thoroughly after each phase before moving to the next.\n